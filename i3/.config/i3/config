# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

focus_on_window_activation focus

set $mod Mod4

exec_always --no-startup-id setxkbmap -option caps:escape
exec_always --no-startup-id ~/.config/polybar/launch.sh
exec_always --no-startup-id ~/.cargo/bin/i3wsr --config ~/.config/i3wsr/config.toml
exec_always --no-startup-id numlockx on

exec_always --no-startup-id picom --experimental-backends
exec_always --no-startup-id feh --bg-scale ~/Pictures/background.png

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:Ubuntu 10

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
exec --no-startup-id dex --autostart --environment i3

# use autolock to suspend after 10 minutes, "notify" by
# locking after 5 minutes with i3lock
exec_always --no-startup-id killall xautolock
exec_always --no-startup-id xautolock -time 10 -locker 'systemctl suspend' -notify 300 -notifier 'i3lock -c 000000 -u -i ~/Pictures/lock_bg.png | xset dpms force off'

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10%
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10%
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle

bindsym XF86AudioPlay exec --no-startup-id playerctl play-pause
bindsym XF86AudioPause exec --no-startup-id playerctl pause
bindsym XF86AudioNext exec --no-startup-id playerctl next
bindsym XF86AudioPrev exec --no-startup-id playerctl previous

# bind calculator key
bindsym XF86Calculator exec gnome-calculator

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# window floating options
bindsym Mod1+Return floating toggle
bindsym Mod5+Return floating toggle

# kill focused window
bindsym $mod+Mod1+v kill
bindsym $mod+Mod5+v kill

# window kill options (mouse)
bindsym --release button2 kill
bindsym --whole-window $mod+Mod1+button2 kill
bindsym --whole-window $mod+Mod5+button2 kill

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Mod1+h move left
bindsym $mod+Mod1+j move down
bindsym $mod+Mod1+k move up
bindsym $mod+Mod1+l move right

bindsym $mod+Mod5+h move left
bindsym $mod+Mod5+j move down
bindsym $mod+Mod5+k move up
bindsym $mod+Mod5+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Mod1+Left move left
bindsym $mod+Mod1+Down move down
bindsym $mod+Mod1+Up move up
bindsym $mod+Mod1+Right move right

bindsym $mod+Mod5+Left move left
bindsym $mod+Mod5+Down move down
bindsym $mod+Mod5+Up move up
bindsym $mod+Mod5+Right move right

# toggle polybar
bindsym $mod+i exec polybar-msg cmd toggle

# split in horizontal orientation
bindsym $mod+Ctrl+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
# bindsym $mod+s layout stacking # disable stacking, don't think I really want to use it ever
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Mod1+space floating toggle
bindsym $mod+Mod5+space floating toggle

# change focus between tiling / floating windows
# bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# use rofi
# bindsym $mod+d exec rofi -show combi
bindsym $mod+space exec rofi -show combi

# printscreen
bindsym Print exec gnome-screenshot -i

# lock
bindsym $mod+Mod1+x exec i3lock -c 000000 -u -i ~/Pictures/lock_bg.png --nofork
bindsym $mod+Mod5+x exec i3lock -c 000000 -u -i ~/Pictures/lock_bg.png --nofork

hide_edge_borders smart

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

set $rosewater          #f5e0dc
set $flamingo           #f2cdcd
set $pink               #f5c2e7
set $mauve              #cba6f7
set $red                #f38ba8
set $maroon             #eba0ac
set $peach              #fab387
set $yellow             #f9e2af
set $green              #a6e3a1
set $teal               #94e2d5
set $sky                #89dceb
set $sapphire           #74c7ec
set $blue               #89b4fa
set $lavender           #b4befe
set $text_white         #fffff0
set $text_black         #100c08
set $text_grey          #d3d3d3
set $subtext1           #bac2de
set $subtext0           #a6adc8
set $overlay2           #9399b2
set $overlay1           #7f849c
set $overlay0           #6c7086
set $surface2           #585b70
set $surface1           #45475a
set $surface0           #313244
set $base               #1e1e2e
set $mantle             #181825
set $crust              #11111b
set $transparent        #00000000
set $focused_window                     #8080807d
set $focused_inactive_window            #5555557d
set $unfocused_window                   #3538397d

# <colorclass>          <border>                        <background>                    <text>                <indicator>     <child_border>
client.focused          $focused_window                 $focused_window                 $text_white           $transparent    $sapphire
client.focused_inactive $focused_inactive_window        $focused_inactive_window        $text_grey            $transparent    $transparent
client.unfocused        $unfocused_window               $unfocused_window               $text_grey            $transparent    $transparent
client.urgent           $transparent                    $transparent                    $peach                $transparent    $transparent
client.placeholder      $transparent                    $transparent                    $text_black           $transparent    $transparent
client.background       $base

default_border pixel 3
default_floating_border normal

for_window [floating] border color $mantle

# gaps
gaps inner 5
gaps outer 10
smart_gaps on

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

bindsym $mod+Mod2+KP_1 workspace number $ws1
bindsym $mod+Mod2+KP_2 workspace number $ws2
bindsym $mod+Mod2+KP_3 workspace number $ws3
bindsym $mod+Mod2+KP_4 workspace number $ws4
bindsym $mod+Mod2+KP_5 workspace number $ws5
bindsym $mod+Mod2+KP_6 workspace number $ws6
bindsym $mod+Mod2+KP_7 workspace number $ws7
bindsym $mod+Mod2+KP_8 workspace number $ws8
bindsym $mod+Mod2+KP_9 workspace number $ws9
bindsym $mod+Mod2+KP_0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Mod1+1 move container to workspace number $ws1
bindsym $mod+Mod1+2 move container to workspace number $ws2
bindsym $mod+Mod1+3 move container to workspace number $ws3
bindsym $mod+Mod1+4 move container to workspace number $ws4
bindsym $mod+Mod1+5 move container to workspace number $ws5
bindsym $mod+Mod1+6 move container to workspace number $ws6
bindsym $mod+Mod1+7 move container to workspace number $ws7
bindsym $mod+Mod1+8 move container to workspace number $ws8
bindsym $mod+Mod1+9 move container to workspace number $ws9
bindsym $mod+Mod1+0 move container to workspace number $ws10

bindsym $mod+Mod5+1 move container to workspace number $ws1
bindsym $mod+Mod5+2 move container to workspace number $ws2
bindsym $mod+Mod5+3 move container to workspace number $ws3
bindsym $mod+Mod5+4 move container to workspace number $ws4
bindsym $mod+Mod5+5 move container to workspace number $ws5
bindsym $mod+Mod5+6 move container to workspace number $ws6
bindsym $mod+Mod5+7 move container to workspace number $ws7
bindsym $mod+Mod5+8 move container to workspace number $ws8
bindsym $mod+Mod5+9 move container to workspace number $ws9
bindsym $mod+Mod5+0 move container to workspace number $ws10

# scratchpad
bindsym $mod+s move scratchpad
bindsym $mod+Mod1+s scratchpad show
bindsym $mod+Mod5+s scratchpad show

# reload the configuration file
bindsym $mod+Mod1+c reload
bindsym $mod+Mod5+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Mod1+r restart
bindsym $mod+Mod5+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Mod1+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"
bindsym $mod+Mod5+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        bindsym $mod+h resize shrink width 100 px or 100 ppt
        bindsym $mod+j resize grow height 100 px or 100 ppt
        bindsym $mod+k resize shrink height 100 px or 100 ppt
        bindsym $mod+l resize grow width 100 px or 100 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

### make make some windows floaters automatically

## QGIS
for_window [title="Data Source Manager"] floating enable
for_window [title="Data Source Manager"] border normal
# attribute table
for_window [title="Features Total"] floating enable
for_window [title="Features Total"] border normal
